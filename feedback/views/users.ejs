<%- include("partials/header") %>
<h2>User List</h2>
<ul>
  <% users.forEach(user => { %>
    <li>
      <%= user.fullname %> - <%= user.email %>

      <a href="/users/<%= user.id %>">Edit</a>
    </li>
  <% }) %>
</ul>

