<%- include("partials/header") %>

<h2>Ticket #<%= ticket.id %></h2>

<p><b>Customer:</b> <%= ticket.customer %></p>
<p><b>Description:</b> <%= ticket.description %></p>
<p><b>Status:</b> <%= ticket.status %></p>
<p><b>Created:</b> <%= ticket.arrived %></p>

<hr>
<h3>Status</h3>

<form method="POST" action="/tickets/status">
  <input type="hidden" name="ticket_id" value="<%= ticket.id %>">

  <select name="status">
    <option value="open" <%= ticket.status === "open" ? "selected" : "" %>>Open</option>
    <option value="working on" <%= ticket.status === "working on" ? "selected" : "" %>>Working on</option>
    <option value="done" <%= ticket.status === "done" ? "selected" : "" %>>Done</option>
    <option value="closed" <%= ticket.status === "closed" ? "selected" : "" %>>Closed</option>
  </select>

  <button type="submit">Save status</button>
</form>

<hr>

<div class="message-list">
  <h3>Messages</h3>

  <% if (!messages || messages.length === 0) { %>
    <p>No messages yet.</p>
  <% } else { %>

    <% messages.forEach(msg => { %>
      <div class="message-item">

        <div class="message-user">
          <%= msg.from_user %>
        </div>

        <div>
          <%= msg.body %>
        </div>

        <div class="message-time">
          <%= new Date(msg.created_at).toLocaleString() %>
        </div>

      </div>
    <% }) %>

  <% } %>
</div>

<hr>

<h3>Add Reply</h3>

<form method="POST" action="/tickets/reply">
  <input type="hidden" name="ticket_id" value="<%= ticket.id %>">

  <textarea name="message" rows="4" style="width:300px;" required></textarea>
  <br><br>

  <button type="submit">Send</button>
</form>

<%- include("partials/footer") %>